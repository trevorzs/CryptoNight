import React from 'react';
import {connect} from 'react-redux';
import WatchlistShares from './watchlist_shares';

const msp = (state, ownProps) => {
  return (
    {

    }
  )
}


const mdp = dispatch => (
  {
  }
);


export default connect(msp,mdp)(WatchlistShares);
